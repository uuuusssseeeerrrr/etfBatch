version: "3.10"

services:
  etfbatch:
    image: etfbatch
    ports:
      - "7777:7777"
    environment:
      - OCI_CONFIG_FILE=/root/.oci/config
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ~/.oci:/root/.oci:ro
